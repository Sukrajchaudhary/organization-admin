# Authentication Configuration
# IMPORTANT: Generate a secure secret using: openssl rand -base64 32
NEXTAUTH_SECRET=your-secure-secret-here-minimum-32-characters

# Application URLs
NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_BASE_SERVER_URL=https://your-api-server.com/api/v1/

# Environment
NODE_ENV=development

# Security Notes:
# 1. NEVER commit .env files to version control
# 2. Use .env.local for local development
# 3. Rotate NEXTAUTH_SECRET regularly in production
# 4. Use different secrets for different environments
# 5. Store production secrets in secure secret management systems